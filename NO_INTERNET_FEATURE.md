# ميزة عدم الاتصال بالإنترنت

## نظرة عامة
تم إضافة ميزة للتحقق من الاتصال بالإنترنت وعرض شاشة خاصة عند عدم وجود اتصال.

## المكونات المضافة

### 1. شاشة عدم الاتصال
- **الملف**: `lib/features/no_internet/presentation/view/no_internet_view.dart`
- **المسار**: `/kNoInternet`
- **الميزات**:
  - تصميم جميل ومتجاوب
  - أيقونة واضحة لعدم الاتصال
  - زر لإعادة المحاولة
  - زر للعودة
  - رسائل باللغة العربية

### 2. خدمة الاتصال
- **الملف**: `lib/core/services/connectivity_service.dart`
- **الميزات**:
  - التحقق من حالة الاتصال
  - الاستماع لتغييرات الاتصال
  - Stream للاستماع للتغييرات

### 3. ConnectivityWrapper
- **الملف**: `lib/core/components/connectivity_wrapper.dart`
- **الوظيفة**: Widget يغلف التطبيق بالكامل للتحقق من الاتصال

## كيفية الاستخدام

### 1. إضافة التبعية
تم إضافة `connectivity_plus: ^6.0.5` إلى `pubspec.yaml`

### 2. إضافة المسار
تم إضافة `kNoInternet` إلى `lib/core/routing/routes_keys.dart`

### 3. إضافة الشاشة إلى المسارات
تم إضافة مسار الشاشة في `lib/core/routing/routes.dart`

### 4. تفعيل الميزة
تم إضافة `ConnectivityWrapper` في `lib/main.dart`

## كيفية عمل الميزة

1. **عند بدء التطبيق**: يتم التحقق من حالة الاتصال
2. **عند فقدان الاتصال**: يتم عرض شاشة عدم الاتصال تلقائياً
3. **عند عودة الاتصال**: يتم العودة للشاشة السابقة تلقائياً
4. **زر إعادة المحاولة**: يتحقق من الاتصال مرة أخرى
5. **زر العودة**: يعود للشاشة السابقة

## التخصيص

### تغيير النصوص
يمكن تعديل النصوص في `NoInternetView`:
```dart
Text(
  'لا يوجد اتصال بالإنترنت', // العنوان
  // ...
),
Text(
  'يرجى التحقق من اتصالك بالإنترنت والمحاولة مرة أخرى', // الرسالة
  // ...
),
```

### تغيير الألوان
يمكن تعديل الألوان باستخدام `AppColors`:
```dart
color: AppColors.primary, // اللون الأساسي
color: AppColors.grey,    // اللون الرمادي
```

### تغيير الأيقونة
يمكن تغيير الأيقونة في `NoInternetView`:
```dart
Icon(
  Icons.wifi_off_rounded, // أيقونة عدم الاتصال
  size: 60,
  color: AppColors.primary,
),
```

## الاختبار

### اختبار عدم الاتصال
1. افتح التطبيق
2. اقطع الاتصال بالإنترنت
3. ستظهر شاشة عدم الاتصال تلقائياً

### اختبار عودة الاتصال
1. في شاشة عدم الاتصال
2. أعد الاتصال بالإنترنت
3. ستختفي الشاشة تلقائياً

### اختبار زر إعادة المحاولة
1. في شاشة عدم الاتصال
2. اضغط على "إعادة المحاولة"
3. سيتم التحقق من الاتصال مرة أخرى

## ملاحظات مهمة

1. **الأذونات**: تأكد من إضافة أذونات الإنترنت في `android/app/src/main/AndroidManifest.xml`:
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
```

2. **iOS**: لا تحتاج لأذونات إضافية في iOS

3. **الأداء**: الخدمة تعمل في الخلفية وتستهلك موارد قليلة

4. **التوافق**: تعمل مع جميع منصات Flutter (Android, iOS, Web) 